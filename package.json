{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "name": "integrator-ui-forms",
  "version": "0.0.1",
  "description": "A forms processor for statically declaring forms with dynamic behaviour",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "postinstall": "yarn build && cp -a packages/core/dist/. ../material-ui-forms-core"
  },
  "license": "MIT",
  "dependencies": {
    "lerna": "^2.11.0"
  }
}
